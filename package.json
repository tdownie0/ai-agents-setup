{
  "name": "model_md_root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "server",
    "client",
    "mcp-database-service",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "db:generate": "npm run db:generate -w @model_md/database",
    "db:migrate": "npm run db:migrate -w @model_md/database",
    "db:seed": "npm run seed -w @model_md/database",
    "db:seed:clear": "npm run seed:clear -w @model_md/database",
    "db:reset": "npm run db:reset -w @model_md/database",
    "test:db": "npm run test -w @model_md/database",
    "supabase:start": "npx supabase start --workdir ./packages/database",
    "supabase:status": "npx supabase status --workdir ./packages/database"
  },
  "devDependencies": {
    "@types/node": "^25.3.3",
    "concurrently": "^9.2.1",
    "supabase": "^2.76.15",
    "typescript": "^5.9.3"
  }
}
